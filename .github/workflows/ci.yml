name: tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: "options-pricing-project/requirements.txt"
      - name: Install dependencies
        run: pip install -r "options-pricing-project/requirements.txt"
      - name: Run test suite
        working-directory: "options-pricing-project"
        run: pytest -q
